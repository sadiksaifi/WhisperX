import Foundation

// MARK: - HotkeyService
/// Listens for global keyboard events to detect push-to-talk activation.
///
/// Threading: Uses a background run loop for event monitoring.
/// Callbacks are dispatched to the main actor for UI updates.
///
/// Requires: Input Monitoring (Accessibility) permission.
final class HotkeyService {
    // TODO: Implement in Step 2
}
